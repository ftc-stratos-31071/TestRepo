# TeleOp

- Registers `Lift` + `Claw` via `SubsystemComponent`.
- Enables `BulkReadComponent` and `BindingsComponent`.
- Uses `MecanumDriverControlled` with GP1 sticks.
- GP2 bindings for lift and claw actions.

